# 内容生产流水线规范 v3.0

> **定位**：本文档是站内容运营的**唯一总纲**。细节标准见 [`blog-seo-geo-standard.md`](./blog-seo-geo-standard.md)，自动化检查见 [`tools/humanize_check.py`](../tools/humanize_check.py)。
>
> v3.0 变更：新增**第 0 阶段「选题与素材采集」**（详见 [`content-sourcing-topic-selection.md`](./content-sourcing-topic-selection.md)），把"去 AI 味 / 人类行为比例"升级为**独立评分维度 + 可执行脚本门禁**，全流程重组为「素材/选题 → 选题卡 → 生产 → 评审 → 修改 → 发布」六阶段。
>
> **两包整合视图（独立成册）**：
> - **《Blog 发布流程包》** → [`packages/blog-publishing-playbook.md`](./packages/blog-publishing-playbook.md)：生产→评审→修改→发布 + 内链 + SEO + GEO
> - **《知识库与关键词选题包》** → [`packages/knowledge-base-topic-selection.md`](./packages/knowledge-base-topic-selection.md)：素材库 SoT + 关键词打分选题

---

## 0. 为什么这是一号工程

### 0.1 因果链：四件事是一条链，不是四件事

```
内容质量 ──▶ 内链结构 ──▶ SEO 权重 ──▶ GEO 引用 ──▶ 免费询盘
   │            │            │            │
   │            │            │            └─ AI 引擎引用你的定义/数据
   │            │            └─ 关键词排名、站点权重、爬虫抓取深度
   │            └─ 权重站内流动，主题集群形成权威
   └─ 唯一不可外包的环节：AI 能生成文字，不能生成你的经验
```

**任何一环断了，后面全部归零**：

- 内容水 → 没人链接、没人引用 → 内链建不起来 → 权重不流动
- 内链断 → 文章是孤岛 → 爬虫抓不深 → 收录都成问题
- SEO 弱 → 没有基础曝光 → 用户数据为零 → GEO 无从谈起
- GEO 弱 → AI 时代的流量入口丢掉 → 2026 年后这部分占比只会更高

### 0.2 战略约束：我们买不起流量

工业 B2B 外销站的现实：

| 渠道                       | 单次点击成本         | 可持续性         | 我们的选择     |
| ------------------------ | -------------- | ------------ | --------- |
| 付费广告（Google Ads / 阿里国际站） | ¥15–60，关键词竞争剧烈 | 停投即停流        | ❌ 补充手段    |
| 展会 / B2B 平台年费            | 数万/年           | 依赖平台         | ❌ 辅助      |
| **内容 + SEO + GEO**       | 单次点击 ≈ 0       | **复利，越久越值钱** | ✅ **主航道** |

**所以这不是"要不要做"的问题，是唯一的路。**  corollary：既然是主航道，就不能靠"有空写一篇"——必须有流水线，让每篇都达到同一标准，让产出可预测。

### 0.3 复利模型（为什么数量 × 质量 × 结构缺一不可）

单篇文章的流量天花板很低。真正的收益来自：

```
30 篇 × 每篇结构化 × 相互链接 × 全部指向产品页
   = 主题权重（topical authority）
   = Google 认为你是「双面胶带」这个主题的权威
   = 新文章发布即获得基础排名（不再从零冷启动）
   = 边际成本递减，边际收益递增
```

**稳定每周 1 篇，胜过一次性发 10 篇然后停 3 个月。** 前者在第 6 个月开始复利，后者在第 2 个月就归零。

### 0.4 一条不可逾越的红线

> **指标服务于真实，不为真实服务指标。**

脚本会统计"第一手经验标记"。**绝不允许为了凑数而编造项目经历、客户反馈、测试数据。** 虚构内容一旦被识破，损失的是整个域名的信任度 —— 而信任度正是 E-E-A-T 和 GEO 的核心。

**没有真实素材时**：该指标记为"待补"，文章照常发布，后续拿到素材再补。不满足指标 ≠ 可以造假。

---

## 1. 流水线总览

```
  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
  │ 0 素材选题│──▶│ 1 选题卡 │──▶│ 2 生产   │──▶│ 3 评审   │──▶│ 4 修改   │──▶│ 5 发布   │
  │ Source/  │   │ Topic   │   │ Draft   │   │ Review  │   │ Revise  │   │ Publish │
  │ Select   │   │ Card    │   │         │   │         │   │         │   │         │
  └─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘
     闸 0          闸 1          自检          闸 3          闸 4?        闸 4
   打分准入      选题准入      （闸 2）      ≥80 才可过    3 次 FAIL     入链闭环
               有业务映射                   ★核心闸       → 作废       + 索引推送
```

| 阶段       | 产出物                               | 闸门                                 | 谁判         |
| -------- | --------------------------------- | ---------------------------------- | ---------- |
| **0 素材/选题** | 关键词 CSV + 素材卡 + 选题卡草稿              | 闸 0：选题打分准入（KD≤40 / 量≥20 / 映射产品∩行业）+ 素材来自 SoT | 人 + 脚本      |
| **1 选题** | 选题卡（主词/长尾/问题词/意图/映射产品）            | 闸 1：无重复、有业务映射才开工                   | 人          |
| **2 生产** | `posts.ts` 新条目（body + faq + 内链字段） | 闸 2：作者自检（骨架 + 去 AI 味）              | 人          |
| **3 评审** | 评分报告                              | **闸 3：5 维加权 ≥80**，脚本 + 人工          | **脚本 + 人** |
| **4 修改** | 修订稿                               | 回到闸 3；**连续 3 次 FAIL → 作废重写**       | 人          |
| **5 发布** | 上线 + 入链                           | 闸 4：build 通过、入链 48h 内补齐、sitemap 收录 | 脚本 + 人     |

### 1.1 各阶段的"不可跳过"

- **闸 1 不能跳**：选题错了，后面写得再好也是无效劳动（没有搜索量 / 不指向产品）。
- **闸 3 不能跳**：这是唯一的质量保证点。凭"我觉得写得不错"发布 = 没有标准。
- **入链不能跳**：发布不入链 = 造孤儿页，等于这一篇的权重收益为零。

### 1.5 阶段零（第 0 阶段）：选题与素材采集

> 完整方法论见 [`content-sourcing-topic-selection.md`](./content-sourcing-topic-selection.md)。本阶段在「阶段一 选题卡」**之前**，解决两个上游问题：**知识从哪来**、**写什么不拍脑袋**。

**素材（知识来源）**：所有技术事实必须来自 `docs/sources/` 素材库（由 `products.ts`、检测报告、`public/reports/*.pdf`、证书抽取），AI 只综合不发明。禁止用训练记忆里的"通识"充当产品技术事实。

**选题（不拍脑袋）**：用 `tools/topic_score.py` 对关键词 CSV 打分排序，三道闸（KD≤40 / 量≥20 / 映射产品∩行业）过滤后，输出编辑日历。

---

## 2. 阶段一：选题

### 2.1 选题卡模板（开工前必须填完）

```markdown
## 选题：<标题草案>

主词（1 个）        : low odor double sided tape
长尾词（3-5）       : low VOC tape automotive / VDA 270 tape / cabin odor adhesive
问题词（5-8）       : what is VDA 270 / how to reduce cabin odor / is acrylic tape low odor
搜索意图            : Informational / Commercial / Transactional
目标字数            : 2000-2500（Commercial 意图）
映射产品（必填）    : low-odor, acrylic, pe-foam
映射行业（必填）    : automotive
买家阶段            : 认知 / 考虑 / 决策
为什么我们能写好    : <我们的独特角度，写不出就换题>
是否已存在相似内容  : 否（已查 posts.ts，无 cannibalization）
```

### 2.2 闸 1 准入条件（全满足才开工）

- [ ] 主词有真实搜索需求（不是自嗨词）
- [ ] **至少映射 1 个产品页**（否则文章无法产生商业价值）
- [ ] 与已有文章无主题重叠（避免自己人打自己人）
- [ ] **"为什么我们能写好"有答案** —— 这一栏空着，说明这题 AI 也能写，竞争对手也能写，写了也白写

---

## 3. 阶段二：生产

### 3.1 骨架：12 段式（照填，不用每次想结构）

```
 1. 定义段          **全称 (缩写)** 开头，前 400 词内全称展开
 2. Key Takeaways   3-5 条自足事实（★ GEO 最高分项，AI 最爱引用）
 3. 背景/为什么重要  业务后果，不是空话
 4. 原理/机理        为什么会这样
 5. 标准与规范       引用真实标准号（VDA / ISO / ASTM / DIN / GB）
 6. 方案对比 A vs B  必须带 ≥1 个表格
 7. 选型因素         3-5 个决策维度
 8. 应用场景         分场景列举，配内链
 9. 规格速查表       ★ ≥5 列 × 8 行，AI 提取友好
10. 实施/验证步骤    标题写成 "How to ..."（触发 HowTo schema）
11. 常见误区         ★ 体现经验：别人踩过的坑
12. FAQ             8-10 条，每条 50-80 词带数据
```

### 3.2 生产时就去 AI 味（事后修比当场写贵 3 倍）

**写作时的四个习惯**：

1. **破折号预算**：写之前记住 —— 全文 `—` 不超过 **6 个/千词**（2000 词 ≈ 12 个）。AI 默认会远超这个数。
2. **每段一句话自问**：「这句话换成竞争对手能写吗？」能 → 删掉或换成只有我们知道的判断。
3. **段落故意不齐**：3 行 / 1 行 / 5 行交替。规整的段落是 AI 的指纹。
4. **每个数据带条件**：不写"耐高温"，写"−20 °C 到 100 °C，夏季暴晒工况下仪表板表面可达 80–100 °C"。

**四类必须出现的"人味"信号**（每项 ≥3 处）：

| 类型             | 例子                                |
| -------------- | --------------------------------- |
| 具体数据 + 单位 + 条件 | "≤ 3.0（VDA 270，40 °C 变体）"         |
| 立场与否定          | "不建议这样做，因为…" / "这里最常被跳过"          |
| 失败案例           | "常见做法是 X，实际会导致 Y"                 |
| 第一手经验          | "我们在 XX 项目发现…"（**必须真实，无素材则留空待补**） |

**对照改写**：

| ❌ AI 写法                                                                 | ✅ 人类写法                                               |
| ----------------------------------------------------------------------- | ---------------------------------------------------- |
| Foam tape is widely used in various industrial applications.            | 门板、顶棚、A 柱这三个位置基本都用泡棉胶带，因为它们是车内面积最大、离乘员最近的地方。         |
| It is important to note that surface preparation affects bond strength. | 表面处理这一步最常被跳过，但现场失效大半能追到它。                            |
| The product offers excellent temperature resistance.                    | −20 °C 到 100 °C。夏天暴晒后仪表板表面能到 80–100 °C，所以上限不能只看常温数据。 |

### 3.3 内链在写作时同步埋（不要留到发布后）

- **正文内联 4–6 个**：锚文本多样化，不要全是产品名
- **`relatedProducts` 3 条**、`relatedIndustries` 1–2 条（字段在 `posts.ts`）
- **权威外链 1–2 条**：链到标准机构（ISO / VDA / ASTM），带 `noopener noreferrer`

---

## 4. 阶段三：评审（★ 核心闸）

### 4.1 五维评分表

| 维度                  | 权重     | 判定方式       | 核心检查项                                                                                                |
| ------------------- | ------ | ---------- | ---------------------------------------------------------------------------------------------------- |
| **Technical SEO**   | 25     | 人工 + 脚本抽查  | title 50-60 / description 150-160 / canonical / 唯一 H1 不跳级 / 图片 alt+尺寸+lazy / OG+Twitter / 三个 JSON-LD |
| **Content SEO**     | 22     | 人工         | 主词覆盖 7 处 / 密度 1-2.5% / LSI 同义词 / 长尾词 / **字数 2000-2500** / TOC / 可扫描性                                 |
| **GEO Readiness**   | 23     | 人工         | **Key Takeaways** / 定义段全称展开 / **≥1 表格** / 直接陈述 / 带单位数据 / **FAQ 8-10 条** / HowTo / E-E-A-T 信号         |
| **Content Quality** | 15     | 人工         | 无填充 / 独特视角 / 可操作 takeaway / 事实准确 / 覆盖完整 / 实例 / 排版                                                    |
| **Humanization**    | **15** | **脚本（客观）** | 见 §4.2                                                                                               |

### 4.2 Humanization 维度（本次升级的核心）

**由 `tools/humanize_check.py` 客观计算，不靠人眼判断。**

```
维度得分 = (humanization / 10) × 15

一票否决：hbr < 15%  →  该维度直接 0 分
扣减：    15% ≤ hbr < 25%  →  该维度得分 × 0.7
```

脚本输出的 5 个统计信号 + 3 个人味信号：

| 指标            | 人类区间      | 说明                      |
| ------------- | --------- | ----------------------- |
| `burstiness`  | 0.60–1.00 | 句长变异系数，AI 写得过于均匀        |
| `cov`         | 0.50–0.90 | 段落长度变异系数                |
| `sttr`        | 0.55–0.80 | 词汇多样性（200 词滑窗）          |
| `trigram_rep` | ≤0.005    | 三词组合重复率                 |
| `slop_per_1k` | ≤3.0      | AI 套路词密度                |
| `dash_per_1k` | ≤6.0      | **破折号密度，本项目最大痛点**       |
| `data_points` | ≥8        | 带单位的数字                  |
| `experience`  | ≥3        | 第一手经验标记（**无真实素材则记待补**）  |
| `stance`      | ≥3        | 立场/否定/警告句               |
| `hbr`         | **≥25%**  | 人类行为比例 = 含人味信号的句子 ÷ 总句数 |

**综合判定**：

```
AI likelihood ≥ 60  或  humanization < 5.5  或  hbr < 15%   →  FAIL，重写
AI likelihood ≥ 40  或  humanization < 7.0  或  hbr < 22%   →  WARN，修改后发
其余                                                        →  PASS
```

### 4.3 评审命令

```bash
PY="C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe"

# 单篇评审
$PY tools/humanize_check.py --slug <slug> --verbose

# 全站扫描（按人性化分升序，最差的排最前）
$PY tools/humanize_check.py --all

# CI 门禁：低于阈值退出码 1
$PY tools/humanize_check.py --slug <slug> --check --threshold 70

# 机器可读（供后续接入自动化）
$PY tools/humanize_check.py --all --json
```

### 4.4 判定

```
综合分 = Σ(维度得分)
90-100  A  优秀
80-89   B  PASS（可发布）
70-79   C  FAIL（需优化）
60-69   D  FAIL（大改）
<60     F  FAIL（重写）
```

**≥80 才可进入阶段五。**

---

## 5. 阶段四：修改

### 5.1 失败项 → 修哪里（速查）

| 失败项                       | 修哪里                        | 怎么修                           |
| ------------------------- | -------------------------- | ----------------------------- |
| **`hbr` < 25%**           | `posts.ts` → `body`        | 加立场句、失败案例、反问句；把定性描述换成带条件的数字   |
| **破折号 > 6/千词**            | `body`                     | `—` 改成逗号、句号、冒号或拆成两句           |
| **`experience` = 0**      | `body`                     | ⚠️ **有真实素材才补**；无则记入待补清单，不造假   |
| **`trigram_rep` > 0.005** | `body`                     | 找出重复三词组合，换同义表达                |
| **字数 < 2000**             | `body`                     | 按 §3.1 骨架补：标准细则 / 场景展开 / 验证步骤 |
| **无 Key Takeaways**       | `body` 第 2 段               | 补 3-5 条自足事实（不依赖上下文也能读懂）       |
| **FAQ < 8 条 / 无数据**       | `faq` 字段                   | 补到 8-10 条，每条 50-80 词带数字       |
| **定义段无全称**                | `body` 首段                  | 改为 `**全称 (缩写)**` 开头           |
| **对比表不足**                 | `body`                     | 补 ≥5 列 × 8 行                  |
| **产品内链不足**                | `body` + `relatedProducts` | 正文内联 4-6 个 + 字段 3 条           |
| **HowTo 缺失**              | `body`                     | 步骤章节标题改为 "How to ..."         |
| **title 超长**              | `title` 字段                 | 压到 ≤60                        |

### 5.2 修改的三条纪律

1. **对着脚本改，不对着感觉改** —— 每改一轮重跑一次，看数字变化，改到达标为止。
2. **连续 3 次 FAIL → 作废重写** —— 不无限修补。修补 3 次还过不了，说明选题或骨架有问题。
3. **不允许为满足指标而编造** —— 见 §0.4。指标可以记"待补"，信任不能透支。

---

## 6. 阶段五：发布与内链闭环

### 6.1 发布清单

```bash
# 1. 构建验证
node ./node_modules/astro/astro.js build

# 2. 页面可达性 + 内容抽查
curl -s http://localhost:8099/blogs/<slug>/ | grep -oE 'Key Takeaways|<table>|faq-item'

# 3. sitemap 收录确认
curl -s http://localhost:8099/sitemap.xml | grep -c '<loc>'

# 4. 人性化门禁（发布前最后一次）
$PY tools/humanize_check.py --slug <slug> --check --threshold 70
```

### 6.2 入链闭环（**发布不是终点**）

> **规则：新文章上线后 48 小时内必须补齐入链。**

```
        ┌──────────────┐
        │  blogs 列表页 │ ← 自动（数据源驱动）
        └──────┬───────┘
               │
   ┌───────────┼───────────┐
   ▼           ▼           ▼
产品详情页   行业详情页    相关文章
（回链）    （回链）      （回链）
```

必须补的三条回链：

| 从                    | 到  | 位置                              |
| -------------------- | -- | ------------------------------- |
| `/products/<映射产品>`   | 本文 | 详情页"Related Resources"区块        |
| `/industries/<映射行业>` | 本文 | 行业页"Further Reading"区块          |
| 同分类其他文章              | 本文 | 详情页底部"Related Posts"（已自动，优先同分类） |

**孤儿检测**：

```bash
# 产品页入站链接 < 2 → 需要补文章指向它
grep -rn "products/<slug>" src/pages src/data | wc -l
```

### 6.3 发布后（部署到公网域名后）

```bash
# GEO 审计（localhost 被 geo 工具安全策略拒绝，需公网域名）
geo audit --url https://<域名>/blogs/<slug>/ --threshold 80

# 全站审计，最弱页面优先（上线后第一件事）
geo audit --sitemap https://<域名>/sitemap.xml --max-urls 50

# 每月：品牌是否被 AI 引用
geo citations --domain <域名>
geo monitor --domain <域名>
```

---

## 7. 工具与资产清单

| 资产           | 路径                                  | 作用             |
| ------------ | ----------------------------------- | -------------- |
| **流水线总纲**    | `docs/content-pipeline.md`          | 本文档，流程入口       |
| **执行细则**     | `docs/blog-seo-geo-standard.md`     | 骨架/GEO/内链的技术细节 |
| **人性化检测脚本**  | `tools/humanize_check.py`           | 零依赖，毫秒级，可接 CI  |
| **选题打分器**     | `tools/topic_score.py`             | 零依赖，输出优先级编辑日历 |
| **素材来源方法论**  | `docs/content-sourcing-topic-selection.md` | 第 0 阶段：SoT 标准 + 选题公式 |
| **素材库**        | `docs/sources/`                    | 由产品/报告抽取的结构化素材卡 |
| **GEO 审计工具** | `geo` CLI（`geo-optimizer-skill`）    | 100 分制，需公网 URL |
| **站点机器可读入口** | `/llms.txt` `/ai/*.json` `/rss.xml` | GEO 基础设施       |
| **站点配置**     | `src/config/site.ts`                | ⚠️ 域名占位，上线前必改  |

### 7.1 为什么自己写脚本而不引 ML 工具

调研过 4 个 GitHub 项目（详见 memory 2026-08-28）：

- 最佳候选 `segraef/humaniser` **404 拿不到**
- 可用的两个（`amine250/ai-detector`、`ankitsharma-tech/generative-ai-detection`）都依赖 **torch/transformers**，GB 级依赖、加载模型几十秒 —— 对"每篇发布前跑一次"属于杀鸡用牛刀
- **AI 味的核心指标本质是纯统计**（句长方差、段落方差、词汇多样性、n-gram 重复、套路词密度），**不需要任何模型**，stdlib 几十行、毫秒级

---

## 8. 节奏与度量

| 频率         | 动作                              | 度量            |
| ---------- | ------------------------------- | ------------- |
| **每周**     | ≥1 篇新文章走完整流水线                   | 发布数、闸 3 一次通过率 |
| **每 3 篇**  | 补入链闭环（产品/行业页回链）                 | 孤儿页数 = 0      |
| **每月**     | `geo citations` + `geo monitor` | 品牌被 AI 引用次数   |
| **每月**     | 全站 `humanize_check --all`       | 平均分趋势（只升不降）   |
| **每季度**    | `geo history` 回归检测              | 分数不回退         |
| **每 6 个月** | 存量文章刷新（更新标准号、数据、内链）             | 刷新覆盖率         |

### 8.1 三个北极星指标

1. **主题集群覆盖率**：`posts.ts` 中每个产品/行业是否都有 ≥1 篇文章指向它
2. **零孤儿页**：产品页入站 ≥2、文章页入站 ≥1
3. **人性化平均分**：`humanize_check --all` 的均值，目标 ≥7.5 且只升不降

---

## 9. 红线与常见错误

### 9.1 三条红线（触碰即报废）

1. **编造数据/案例/客户反馈** —— 透支域名信任，E-E-A-T 直接归零
2. **跳过闸 3 发布** —— 没有标准 = 没有质量，等于把主航道交给运气
3. **发布不入链** —— 该篇权重收益为零，等于白写

### 9.2 常见错误

| 错误                | 后果        | 正确做法           |
| ----------------- | --------- | -------------- |
| 选题不映射产品           | 有流量无询盘    | 闸 1 强制填"映射产品"  |
| 先写再想 SEO          | 结构返工      | 选题卡先定主词/长尾     |
| 事后补 Key Takeaways | 写不出真正的要点  | 骨架第 2 段就是它     |
| 为凑经验指标编造          | 信任破产      | 记"待补"          |
| 破折号滥用             | AI 味最显著特征 | 写作时就有预算（≤6/千词） |
| 段落长度整齐            | AI 指纹     | 故意不齐           |
| 只做出链不做入链          | 权重只出不进    | 48h 规则         |
| 凭感觉判断"写得不错"       | 无标准       | 跑脚本、看数字        |
| 无限修补不达标文章         | 沉没成本      | 3 次 FAIL 作废    |

---

## 10. 当前状态

### 10.1 已完成 ✅

- [x] 流水线总纲（本文档）
- [x] 执行细则 v2.0（骨架 / GEO / 内链 / 工具链）
- [x] **人性化检测脚本**（5 统计信号 + 3 人味信号 + HBR + 门禁模式）
- [x] SEO 基础设施（canonical / OG / JSON-LD / sitemap / robots）
- [x] GEO 基础设施（llms.txt / ai/*.json / rss.xml / .well-known/ai.txt）
- [x] 首篇范文：`low-odor-tape-automotive-interiors`
- [x] **第 0 阶段方法论**：`content-sourcing-topic-selection.md`（SoT 来源标准 + 选题打分公式）
- [x] **选题打分器**：`tools/topic_score.py`（零依赖，自动比对产品/行业算 Fit）
- [x] **素材库**：`docs/sources/`（acrylic / low-odor / pe-foam / flame-retardant / pet 五张卡，数据来自 `products.ts` + 真实检测报告）

### 10.2 待办 ⬜

| 项                        | 状态     | 阻塞原因                                               |
| ------------------------ | ------ | -------------------------------------------------- |
| 首篇达标（≥80）                | 🔄 进行中 | 字数 1277→2000、破折号 21.9→≤6、HBR 18.9%→≥25%            |
| `experience` 指标          | ⬜      | **需真实素材**，无则不补（记待补）                                |
| 剩余 9 篇正文                 | ⬜      | 按流水线逐篇产出                                           |
| 入链闭环（产品/行业 → 文章）         | ⬜      | 需建"Related Resources"区块                            |
| 域名替换                     | ⬜      | `src/config/site.ts` 的 `https://lubandart.com` 是占位 |
| Brand & Entity（GEO 10 分） | ⬜      | 需提供 Wikidata / LinkedIn / Crunchbase 真实链接          |
| 真实关键词数据接入            | ⬜      | 用 Ahrefs / SEMrush / Search Console 导出真实 volume/cpc/kd 替换 `keywords.sample.csv` 示意值 |

### 10.3 首篇基线（2026-08-29 实测）

```
humanization      7.50 / 10    ← 达标线边缘
AI likelihood     20.4 / 100   ← 良好
human behaviour   18.9 %       ← 未达标（目标 ≥25%）

ok     : burstiness 0.84 · cov 0.748 · sttr 0.721 · slop 0.0 · data_points 10 · stance 11
LOW    : experience 0 · hbr 18.9%
HIGH   : trigram_rep 0.0118 · dash/1k 21.93
words  : 1277 / 2000-2500
```

**这份基线本身就是脚本价值的证明** —— 手写时判断"写得挺好"，数字一出，三个具体问题立刻暴露。
